[package]
name = "autocommit-core"
version = "0.10.1"
edition = "2024"

[features]
default = []
llama-native = []

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
